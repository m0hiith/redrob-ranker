# Core pipeline — all models are local (no hosted API calls permitted during ranking).
# sentence-transformers loads BAAI/bge-small-en-v1.5 on first run (auto-cached).
sentence-transformers>=2.6.0
numpy>=1.24.0
