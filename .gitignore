# Vendored model (recreate with: python download_model.py).
# Bare name also covers a local `models` symlink used during development.
models/
models

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/

# OS noise
.DS_Store

# Run outputs (committed examples: smoke_out.csv only)
run_out.csv
submission.csv
*.csv.tmp
embeddings_cache.npy
